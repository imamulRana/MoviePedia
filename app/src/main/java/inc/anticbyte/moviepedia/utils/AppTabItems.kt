package inc.anticbyte.moviepedia.utils

enum class AppTabItems(val tabName: String, val timeWindow: String) {
    DAY(tabName = "Day", timeWindow = "day"),
    WEEK(tabName = "Week", timeWindow = "week"),
}